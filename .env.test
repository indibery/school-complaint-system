# 테스트 환경 변수
NODE_ENV=test
PORT=3001

# 테스트 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=school_complaint_test
DB_USER=postgres
DB_PASSWORD=password

# JWT 시크릿 키 (테스트용)
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production
JWT_REFRESH_SECRET=test-refresh-secret-key-for-testing-only-do-not-use-in-production

# 이메일 설정 (테스트용)
EMAIL_HOST=smtp.test.com
EMAIL_PORT=587
EMAIL_USER=test@test.com
EMAIL_PASS=test-password
FROM_EMAIL=test@school.com
FROM_NAME=Test School System

# 보안 설정
BCRYPT_ROUNDS=10
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=30
BRUTE_FORCE_WINDOW=15
BRUTE_FORCE_MAX_ATTEMPTS=20

# 토큰 설정
ACCESS_TOKEN_EXPIRY=24h
REFRESH_TOKEN_EXPIRY=7d

# 기타 설정
FRONTEND_URL=http://localhost:3000
API_BASE_URL=http://localhost:3001
